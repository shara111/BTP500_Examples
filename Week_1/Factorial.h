#ifndef RECURSION_EXAMPLE_H
#define RECURSION_EXAMPLE_H

#include <iostream>

/*
* This is a classic example of a problem that can be solved
* with recursion. 
* Factorials are defined as "products" (multiples) of a number
* and all of its preceeding positive integers.
*
* Factorials follow the formula of:
* n! = n * (n-1) * (n-2) * ... * 2 * 1
*/

int factorial(int);

#endif