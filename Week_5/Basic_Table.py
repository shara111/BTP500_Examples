'''
Here is the most basic table we can make in Python.
(using a dictionary which is hashed, thus destroying
the point in a fiery blaze)
'''

if __name__ == "__main__":
    # A simple data table using a dictionary
    # If there were any  reason to disregard everything I teach,
    # this example may be it.
    data_table = {
        'apnle': 3,
        'stawbry': 5,
        'chewwie owo': 2
    }

    # A simpler table, array-based
    simple_table = [0] * 10  # Fixed size of 10 

    # Storing some data
    simple_table[0] = 'apnle'
    simple_table[1] = 'pear' 
